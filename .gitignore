node_modules
.next
.env
.env.local
*.db
*.db-shm
*.db-wal
*.db-journal
data/*.csv
data/*.json
# Per-run span logs and the Neatlogs debug dump, regenerated on every close.
# (git has no inline comments — a trailing one becomes part of the pattern.)
data/traces/
spans_raw_optimized.log
.DS_Store

# Internal working material: battle plan, demo scripts, voice-over, submission
# drafts. Public-facing architecture and flow docs live in docs/ instead.
private/

# build artefacts
tsconfig.tsbuildinfo
next-env.d.ts
